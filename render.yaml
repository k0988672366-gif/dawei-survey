services:
  - type: web
    name: class-survey-ai-agent
    runtime: python
    buildCommand: "pip install -r requirements.txt || true"
    startCommand: "python3 survey_server.py"
    envVars:
      - key: PORT
        value: 10000
